
Define the skeleton of an algorithm in an operation, deferring some steps to subclasses.
Template Method lets subclasses redefine certain steps of an algorithm without changing the algorithm's structure.
-GOF Design Patterns

There are following lists the benefits of using the Template pattern:
    *   It reduces the boilerplate codes in the application by reusing code.
    *   This pattern creates a template or way to reuse multiple similar algorithms to perform some
     business requirements.

Dinesh Rajput. Spring 5 Design Patterns (Kindle Locations 2402-2404). Packt.